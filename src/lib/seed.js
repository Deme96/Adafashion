// ========== Ada Fashion Seed Data ==========
// This file is no longer used. All seed data is managed server-side
// by the MySQL backend in server/index.js (seedDefaultData function).
// The localStorage-based approach has been fully replaced by MySQL.

export const seedDatabase = () => {
  // No-op: seeding is handled by the server on startup.
  console.log('Seed skipped — data is managed by the MySQL backend.');
};
